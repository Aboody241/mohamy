import 'package:flutter/material.dart';

class AppColors {
  // ألوان أساسية
  static const Color primary = Color(0xFF3949AB); // أزرق غامق شوية
  static const Color primaryLight = Color(0xFF6F74DD);
  static const Color primaryDark = Color(0xFF00227B);

  static const Color secondary = Color(0xFFFFB300); // أصفر/ذهبي بسيط
  static const Color accent = Color(0xFF26A69A); // أخضر مزرق خفيف

  // ألوان الحالة
  static const Color success = Color(0xFF43A047);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA000);
  static const Color info = Color(0xFF1E88E5);

  // محايد / خلفيات
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color border = Color(0xFFE0E0E0);

  // نصوص
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF616161);
  static const Color textDisabled = Color(0xFF9E9E9E);
}
